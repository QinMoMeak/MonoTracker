# Tracker 1.5.31

## 本次更新

### 国际化文案乱码修复
- 修复 `constants.ts` 中历史编码污染导致的大量中文、繁体中文和日文文案异常。
- 语言切换、快速开始、AI 设置、WebDAV、筛选面板、统计页等高频区域的文本已恢复为正常可读内容。
- 保留此前已经修复的语言名称、`lastUsedDate`、`daysUsed`、`webdavNativeOnly` 等关键文案，避免再次回退到损坏状态。

### 国际化基线收口
- 以较新的干净文案版本为基线重新整理 `TEXTS`，保持当前主题系统、分类配色和功能字段不变。
- 文案文件继续使用 UTF-8 保存，减少后续构建或编辑过程中再次发生转码污染的风险。

## 验证
- 前端生产构建通过。
- Android Debug APK 构建通过。
- 当前版本为 `1.5.31`，`versionCode` 为 `50`。
